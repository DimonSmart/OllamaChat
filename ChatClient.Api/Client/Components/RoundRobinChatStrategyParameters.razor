@using MudBlazor
@using ChatClient.Domain.Models.ChatStrategies
@inherits ChatStrategyParametersBase

<MudNumericField T="int"
                 Value="TypedOptions.Rounds"
                 ValueChanged="OnRoundsChanged"
                 Min="1"
                 Label="Rounds"
                 Variant="Variant.Outlined"
                 FullWidth="true"
                 Class="mt-4" />

@code {
    private RoundRobinChatStrategyOptions TypedOptions => (RoundRobinChatStrategyOptions)Context.Options;

    private void OnRoundsChanged(int value)
    {
        TypedOptions.Rounds = value;
    }
}
